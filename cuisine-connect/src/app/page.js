import { Box, Sheet } from '@mui/joy'
import Image from 'next/image'

export default function Home(
  params, search
) {
  return (
    <Box>
      <Sheet>
        Home page
        {/* Receipt Card list */}
      </Sheet>
    </Box>
  )
}
